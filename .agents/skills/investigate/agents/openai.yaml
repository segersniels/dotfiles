interface:
  display_name: "Investigate"
  short_description: "Read-only investigation and context catch-up"
  default_prompt: "Use $investigate to establish the cause and evidence, report material uncertainty, and continue any implementation already authorized."

policy:
  allow_implicit_invocation: true
