interface:
  display_name: "Commit"
  short_description: "Create small, conventional Git commits"
  default_prompt: "Use $commit to create focused Conventional Commits from the completed, validated changes within the requested scope."
