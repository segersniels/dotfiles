interface:
  display_name: "Orchestrate"
  short_description: "Coordinate focused parallel implementation workers"
  default_prompt: "Use $orchestrate to implement the plan with focused parallel workers."
policy:
  allow_implicit_invocation: false
