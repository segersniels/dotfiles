interface:
  display_name: "Review"
  short_description: "Adaptive parallel evidence-driven review"
  default_prompt: "Use $review to review these changes with adaptive, evidence-driven coverage."

policy:
  allow_implicit_invocation: false
